<svg class="box">
<!-- TODO: Move to SCSS -->
  <style>
    .box {
      border: 1px black solid;
      width: 300px;
      height: 500px;
    }
    text {
      user-select: none;
    }
  </style>
  <circle (mousedown)="startDrag($event)" cx="24" cy="100" r="24" fill="orange" />
</svg>


<pre style="user-select: none">
  <h2>{{log | json}}</h2>
</pre>
